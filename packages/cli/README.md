# @ergon/cli

Globally installable CLI for Ergon Flow.

Install:

```bash
pnpm add -g @ergon/cli
```

Quickstart:

```bash
ergon init
ergon template list
```

Repository:

- https://github.com/claushaas/ergon-flow
